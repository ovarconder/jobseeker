# 🔧 แก้ปัญหา Vercel สับสน Git Account

## ปัญหาที่พบ

Vercel เชื่อมต่อกับ GitHub account ผิด หรือมีหลาย accounts และเลือก account ผิด

## วิธีแก้ไข

### ขั้นตอนที่ 1: ตรวจสอบ GitHub Account ที่เชื่อมต่ออยู่

1. ไปที่ [Vercel Dashboard](https://vercel.com/dashboard)
2. ไปที่ **Settings** (ไอคอนฟันเฟืองด้านบนขวา) → **Git**
3. ดูว่าเชื่อมต่อกับ GitHub account ไหน
4. ตรวจสอบว่าเป็น account ที่ถูกต้องหรือไม่

### ขั้นตอนที่ 2: Disconnect GitHub Account ผิด

1. ไปที่ Vercel Dashboard → **Settings** → **Git**
2. หา GitHub integration
3. คลิก **Disconnect** หรือ **Remove**
4. ยืนยันการ disconnect

### ขั้นตอนที่ 3: เชื่อมต่อ GitHub Account ที่ถูกต้อง

1. ไปที่ Vercel Dashboard → **Settings** → **Git**
2. คลิก **Connect Git Provider** หรือ **Add Git Provider**
3. เลือก **GitHub**
4. เลือก GitHub account ที่ถูกต้อง (ถ้ามีหลาย accounts)
5. Authorize Vercel ให้เข้าถึง GitHub
6. เลือก repositories ที่ต้องการให้ Vercel เข้าถึง

### ขั้นตอนที่ 4: เชื่อมต่อ Project กับ Repository ใหม่

1. ไปที่ Vercel Dashboard → **Project** → **Settings** → **Git**
2. ตรวจสอบว่าเชื่อมต่อกับ repository ถูกต้อง:
   - ควรเป็น: `ovarconder/jobseeker`
   - หรือ: `YOUR_CORRECT_USERNAME/jobseeker`
3. ถ้าไม่ถูกต้อง:
   - คลิก **Disconnect**
   - คลิก **Connect Git Repository**
   - เลือก GitHub account ที่ถูกต้อง
   - เลือก repository: `jobseeker`
   - คลิก **Connect**

### ขั้นตอนที่ 5: Trigger Deployment ใหม่

หลังจากเชื่อมต่อถูกต้องแล้ว:

1. **วิธีที่ 1: Push commit ใหม่**
   ```bash
   git commit --allow-empty -m "Reconnect Vercel with correct GitHub account"
   git push
   ```

2. **วิธีที่ 2: Redeploy ใน Vercel**
   - ไปที่ Vercel Dashboard → **Deployments**
   - เลือก deployment ล่าสุด
   - คลิก **...** → **Redeploy**

## ถ้ามีหลาย GitHub Accounts

### วิธีจัดการหลาย Accounts

1. **ใน Vercel**:
   - ไปที่ Settings → Git
   - ดู accounts ที่เชื่อมต่ออยู่
   - Disconnect account ที่ไม่ต้องการ
   - Connect account ที่ถูกต้อง

2. **ใน GitHub**:
   - ตรวจสอบว่า repository อยู่ใน account ไหน
   - ตรวจสอบว่า Vercel มีสิทธิ์เข้าถึง repository หรือไม่

### ตรวจสอบ Repository Owner

```bash
# ตรวจสอบ remote URL
git remote -v

# ดูว่า repository อยู่ใน account ไหน
# ตัวอย่าง: https://github.com/ovarconder/jobseeker.git
#          ^^^^^^^^
#          account name
```

## วิธีตรวจสอบว่าเชื่อมต่อถูกต้อง

### 1. ตรวจสอบใน Vercel Dashboard

- ไปที่ Project → **Settings** → **Git**
- ดูว่าเชื่อมต่อกับ repository: `USERNAME/jobseeker`
- ตรวจสอบว่า USERNAME ถูกต้อง

### 2. ตรวจสอบใน GitHub

- ไปที่ GitHub repository
- ตรวจสอบว่า repository อยู่ใน account ที่ถูกต้อง
- ตรวจสอบว่า Vercel มีสิทธิ์เข้าถึง (ถ้าเป็น private repo)

### 3. ทดสอบ Push

```bash
# Push code
git push

# ตรวจสอบใน Vercel Dashboard ว่ามี deployment ใหม่หรือไม่
```

## Troubleshooting

### ปัญหา: Vercel ไม่เห็น Repository

**วิธีแก้:**
1. ตรวจสอบว่า repository เป็น public หรือ Vercel มีสิทธิ์เข้าถึง
2. ตรวจสอบว่าเชื่อมต่อกับ GitHub account ที่ถูกต้อง
3. ลอง disconnect และ connect ใหม่

### ปัญหา: Vercel ไม่ Trigger Deployment

**วิธีแก้:**
1. ตรวจสอบว่าเชื่อมต่อกับ repository ถูกต้อง
2. ตรวจสอบว่า push ไปยัง branch ที่ถูกต้อง (main/master)
3. ตรวจสอบ Vercel Settings → Git → Production Branch

### ปัญหา: Permission Denied

**วิธีแก้:**
1. ตรวจสอบว่า Vercel มีสิทธิ์เข้าถึง repository
2. ไปที่ GitHub → Settings → Applications → Authorized OAuth Apps
3. ตรวจสอบว่า Vercel มีสิทธิ์เข้าถึง repository

## Checklist

- [ ] ตรวจสอบ GitHub account ที่เชื่อมต่อใน Vercel
- [ ] Disconnect account ผิด (ถ้ามี)
- [ ] Connect GitHub account ที่ถูกต้อง
- [ ] เชื่อมต่อ Project กับ repository ที่ถูกต้อง
- [ ] ตรวจสอบว่า repository owner ถูกต้อง
- [ ] Push code เพื่อ trigger deployment
- [ ] ตรวจสอบว่า Vercel compile สำเร็จ

## คำสั่งที่ใช้ตรวจสอบ

```bash
# ตรวจสอบ remote URL
git remote -v

# ดู repository owner
# จาก URL: https://github.com/OWNER/repo.git

# Push เพื่อ trigger deployment
git push
```

## สรุป

1. ✅ ไปที่ Vercel Dashboard → Settings → Git
2. ✅ Disconnect GitHub account ผิด
3. ✅ Connect GitHub account ที่ถูกต้อง
4. ✅ เชื่อมต่อ Project กับ repository ที่ถูกต้อง
5. ✅ Push code เพื่อ trigger deployment

ลองทำตามขั้นตอนด้านบนเพื่อแก้ไขปัญหา Vercel สับสน git account ครับ!
