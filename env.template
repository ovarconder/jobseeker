# ============================================
# Environment Variables Template
# ============================================
# คัดลอกไฟล์นี้เป็น .env และกรอกข้อมูลที่จำเป็น
# คำสั่ง: cp env.template .env

# ============================================
# Database Configuration
# ============================================
# สำหรับ Local Development: ใช้ local PostgreSQL
# DATABASE_URL="postgresql://user:password@localhost:5432/jobmatch"

# สำหรับ Supabase: ใช้ Connection String จาก Supabase Dashboard
# ไปที่: Supabase Dashboard → Settings → Database → Connection string → URI
# DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# สำหรับ Production (Vercel): ใช้ Connection Pooling (แนะนำ)
# DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres"

DATABASE_URL="postgresql://user:password@localhost:5432/jobmatch"

# ============================================
# NextAuth Configuration
# ============================================
# สำหรับ Local Development
NEXTAUTH_URL="http://localhost:3000"

# สำหรับ Production: เปลี่ยนเป็น URL ของ Vercel
# NEXTAUTH_URL="https://your-project.vercel.app"

# สร้าง Secret Key ด้วยคำสั่ง: openssl rand -base64 32
NEXTAUTH_SECRET="change-this-to-a-random-secret-key"

# ============================================
# LINE Bot Configuration (Server-side)
# ============================================
# ไปที่: LINE Developers Console → Messaging API → Channel settings
LINE_CHANNEL_ACCESS_TOKEN="your-line-channel-access-token"
LINE_CHANNEL_SECRET="your-line-channel-secret"

# ============================================
# LINE LIFF Configuration
# ============================================
# ไปที่: LINE Developers Console → LIFF → Create LIFF app
# ใช้ค่าเดียวกันสำหรับทั้งสองตัวแปร (LIFF App ID)

# Server-side (ใช้ใน webhook)
LINE_LIFF_ID="your-liff-app-id"

# Client-side (ใช้ใน LIFF pages - ต้องมี NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_LIFF_ID="your-liff-app-id"

# ============================================
# LINE Login Configuration (OAuth)
# ============================================
# ไปที่: LINE Developers Console → LINE Login → Create Channel
# ใช้ Channel ID และ Channel Secret จาก LINE Login Channel (ไม่ใช่ Messaging API)
LINE_LOGIN_CHANNEL_ID="your-line-login-channel-id"
LINE_LOGIN_CHANNEL_SECRET="your-line-login-channel-secret"

# เปิดใช้งาน LINE Login button (ตั้งเป็น "true" เพื่อแสดงปุ่ม LINE Login)
NEXT_PUBLIC_LINE_LOGIN_ENABLED="true"
